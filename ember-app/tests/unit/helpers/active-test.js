import { active } from '../../../helpers/active';
import { module, test } from 'qunit';

module('Unit | Helper | active');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = active(42);
  assert.ok(result);
});
