---
title: "Thoughts"
altTitle: "I have thoughts."
description: "This area of my website is intended to act somewhat like a blog. But none of these are really blog posts or articles. They are more like a collection of thoughts and experiments."
date: 2016-09-01
priority: 0.9
pageClasses:
  - 'thoughts'
  - 'index'
layout: base
styles:
  - 'main'
  - 'thoughts/index'
---

<section>
  <ul id="thoughts-list" class="content">
    <%
      const basePages = getChildPages('thoughts');
      const newestPages = basePages.sort((pageA, pageB) => {
        const dateA = new Date(pageA.data.date);
        const dateB = new Date(pageB.data.date);
        return (dateA < dateB) ? 1 : -1;
      });
      newestPages.forEach(({ url, data: { date, description, title }}) => {
    %>
    <li itemscope itemtype="http://schema.org/BlogPosting">
      <a itemprop="url" href="/<%- url %>">
        <h3 itemprop="name"><%- noWidows(title) %></h3>
        <figcaption itemprop="description">
          <%- noWidows(description) %>
        </figcaption>
        <date itemprop="datePublished" content="<%- date %>">
          <%- formattedDate(date) %>
        </date>
        <meta itemprop="author" content="Brian Anders"/>
        <meta itemprop="publisher" content="Brian Anders"/>
      </a>
    </li>
    <% }); %>
  </ul>
</section>
