{{#each albums as |album index|}}
  {{#if (lt index length)}}
    {{#if showDescription}}
      <div class="album-tile">
        <a itemprop="url" target="_blank" href="{{album.url}}" class="album" title="{{album.name}}">
          {{image-tag src=album.image alt=album.name }}
        </a>
        <ul>
          <h3 class="name">
            <a itemprop="url" target="_blank"
               href="{{album.url}}"
               title="{{album.name}}">
              {{album.name}}
            </a>
          </h3>
          <li>By: <a itemprop="url" href="{{album.artist.url}}" title="{{album.artist.name}}" target="_blank">{{album.artist.name}}</a></li>
        </ul>
      </div>
    {{else}}
      <a itemprop="url" target="_blank" href="{{album.url}}" class="album" title="{{album.name}}">
        {{image-tag src=album.image alt=album.name }}
      </a>
    {{/if}}
  {{/if}}
{{/each}}
