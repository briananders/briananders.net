<link rel="stylesheet" media="all" href="/styles/modules/header.css">

<div id="skip-nav" tabindex="-1" data-state=inactive>
  <button>Skip navigation</button>
</div>
<%- partial('nav', locals); %>
<% if(!locals.homepage) { %>
  <header class="main-header <%- (locals.pageClasses) ? locals.pageClasses.toString().replace(/,/g,' ') : '' %>">
    <h1 itemprop="title" class="name content">
      <% if (locals.altTitle !== undefined) { %>
        <%- locals.altTitle %>
      <% } else if (locals.title !== undefined) { %>
        <%- locals.title %>
      <% } %>
    </h1>
  </header>
<% } %>
